#2. Write a program to calculate area of rectangle based on length and breadth.
len = float(input('enter the lenth : '))
bre = float (input('enter the breath : '))

area_of_rectangle = len *bre

print(f"area of rectangle is : {area_of_rectangle} ")